import React, { useState } from 'react';

const LikeButton = () => {
    // 여기에 코드 작성
    // 1. useState로 좋아요 수 상태 만들기 (초기값: 0)
    // 2. 증가 함수 만들기
    // 3. 초기화 함수 만들기

    return (
        <div>
            {/* 좋아요 수 표시 */}
            {/* 하트 버튼 */}
            {/* 10 이상이면 메시지 표시 */}
            {/* 초기화 버튼 */}
        </div>
    );
}

export default LikeButton;